EXAMPLE VIDEO ENCODER DEFINITION FILE NEEDS UPDATING - FILL OUT ALL THE VARIABLES, NOT JUST THE ONES USED BY MY PRESET.


vencsw type case handling code could be rewritten into helper functions to declutter both main.cpp and preset.cpp
main.cpp type case handling code needs a boundary check update. actually, shared helper-function would need that.

File existance checking, and directory listing code could be moved to a helper function to clean main.cpp up.




Possible sw_v3 (v2 with slight cleanup utiliting std::unsorted_map to store the name in one place) could be done, hell, that could also be a good idea for implementing audio support.
Possible cleanup with separating definition and set values, would possibly use slightly more memory, BUT WOULD ENSURE DEFINITIONS WON'T GET OVERWRITTEN.
sw_v2 ASSUMES THAT THERE'S ONLY ONE FUNCTION THAT CAN EXCLUDE ANOTHER ONE. DAMN.
handle_multivar() assumes the devider is "," and there's only INT OR FLOATING POINT EVER USED THERE. Possibly rewrite in the future with reading character by character, which while slower would allow for far greater flexability in creating presets.

MOVE ALL THE COMMON DEFINES INTO ONE EXTERNAL CPP FILE, WHEN DEFINE FAILS TO FUNCTION.


audio encoder preset and handling spec TBD.
MUXING SUPPORT TBD.
VS-based rewrite TBD
HEADER MINIMIZATION TBD.
HDR10 / HDR10+ handling TBD

Parse_def():
type -1 works
type 0 works
type 1 works
type 2 REMAINS UNTESTED
type 3 works
type 4 works
